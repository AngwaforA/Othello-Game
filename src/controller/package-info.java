/**
 * Package of the controller class for the model {@Link model.GameBoard}.
 * <p>
 * The controller handles the user input, modifies the model, and decides what the view should draw.
 * <p>
 * This package contains the view and controller interfaces, since they are required to use the controller.
 *
 * @author Ashley Angwafor
 */
package controller;